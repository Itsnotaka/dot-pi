# Parallel Websearch Extension

Registers a `websearch` tool that calls Parallel's Search API.

## Requirements

- Set `PARALLEL_API_KEY` in your environment.

## Usage

```
Use websearch to find the Parallel Search API quickstart.
```
